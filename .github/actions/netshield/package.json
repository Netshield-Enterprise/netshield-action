{
  "name": "netshield",
  "version": "1.0.0",
  "description": "Zero-configuration secrets detection for GitHub PRs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build src/index.ts -o dist",
    "test": "jest"
  },
  "keywords": [
    "github-action",
    "secrets",
    "security"
  ],
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
